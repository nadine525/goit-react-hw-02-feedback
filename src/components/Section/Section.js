import React from 'react';
import { Division } from './Section.styled';

const Section = ({ title }) => {
  <Division>
    <h2>{title}</h2>
    {/* {children} */}
  </Division>;
};

export default Section;
